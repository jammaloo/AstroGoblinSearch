# Audio (transient: downloaded per-video, then deleted after transcription)
/audio/

# SQLite sidecar/lock files are transient — only the main DB is committed (via LFS).
*.db-journal
*.db-wal
*.db-shm
*.log

# Python
__pycache__/
*.pyc

# venv
.venv/
venv/

# whisper model cache
*.pt
